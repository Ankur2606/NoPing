{
  "name": "hardhat-project",
  "version": "1.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "deploy-local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy-testnet": "npx hardhat run scripts/deploy.js --network bnbTestnet",
    "deploy-mainnet": "npx hardhat run scripts/deploy.js --network bnbMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.24.0"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
